'use client';

const error = () => {
    return (
        <div className="fixed inset-0 flex items-center justify-center bg-opacity-50">
            <div>Main Page Error</div>
        </div>
    );
};

export default error;